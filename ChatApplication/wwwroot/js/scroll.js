var targetDiv = document.querySelector('.chat-box');
targetDiv.scrollTop = targetDiv.scrollHeight;

//$(document).ready(function () {
//    $('.chat-box').scrollTop( $('.chat-box')[0].scrollHeigh );
//});